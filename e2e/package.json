{
  "name": "usb-video-vault-e2e",
  "version": "1.0.0",
  "description": "E2E tests for USB Video Vault",
  "scripts": {
    "install-playwright": "npm install @playwright/test playwright",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}